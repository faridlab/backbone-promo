//! Public services for Promo module
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! These services provide the public API for other modules.
//! They only expose read operations - writes go through events.

use std::sync::Arc;

use anyhow::Result;
use async_trait::async_trait;
use uuid::Uuid;

use super::types::*;

// ============================================================================
// QUERY SERVICE TRAIT
// ============================================================================

/// Public query service for Promo module
///
/// This trait defines read-only operations that other modules can use.
/// Implementations should NOT expose internal domain logic.
#[async_trait]
pub trait PromoQueryService: Send + Sync {
    /// Get AdPerformance by ID
    async fn get_ad_performance(&self, id: AdPerformanceId) -> Result<Option<AdPerformanceDto>>;

    /// Get AdPerformance summary by ID
    async fn get_ad_performance_summary(&self, id: AdPerformanceId) -> Result<Option<AdPerformanceSummary>>;

    /// Check if AdPerformance exists
    async fn ad_performance_exists(&self, id: AdPerformanceId) -> Result<bool>;

    /// Get Advertisement by ID
    async fn get_advertisement(&self, id: AdvertisementId) -> Result<Option<AdvertisementDto>>;

    /// Get Advertisement summary by ID
    async fn get_advertisement_summary(&self, id: AdvertisementId) -> Result<Option<AdvertisementSummary>>;

    /// Check if Advertisement exists
    async fn advertisement_exists(&self, id: AdvertisementId) -> Result<bool>;

    /// Get ProviderPromo by ID
    async fn get_provider_promo(&self, id: ProviderPromoId) -> Result<Option<ProviderPromoDto>>;

    /// Get ProviderPromo summary by ID
    async fn get_provider_promo_summary(&self, id: ProviderPromoId) -> Result<Option<ProviderPromoSummary>>;

    /// Check if ProviderPromo exists
    async fn provider_promo_exists(&self, id: ProviderPromoId) -> Result<bool>;

    /// Get PromoCampaign by ID
    async fn get_promo_campaign(&self, id: PromoCampaignId) -> Result<Option<PromoCampaignDto>>;

    /// Get PromoCampaign summary by ID
    async fn get_promo_campaign_summary(&self, id: PromoCampaignId) -> Result<Option<PromoCampaignSummary>>;

    /// Check if PromoCampaign exists
    async fn promo_campaign_exists(&self, id: PromoCampaignId) -> Result<bool>;

}

// ============================================================================
// QUERY SERVICE IMPLEMENTATION
// ============================================================================

/// Default implementation of PromoQueryService
pub struct PromoQueryServiceImpl<R> {
    repository: Arc<R>,
}

impl<R> PromoQueryServiceImpl<R> {
    pub fn new(repository: Arc<R>) -> Self {
        Self { repository }
    }
}

// ============================================================================
// CUSTOM SERVICES
// ============================================================================

// <<< CUSTOM SERVICES START >>>
// Add custom public services here
// <<< CUSTOM SERVICES END >>>
