//! LoyaltyOrderPoints API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the LoyaltyOrderPoints CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// LOYALTYORDERPOINTS TEST DATA GENERATOR
// ============================================================================

/// Test data generator for LoyaltyOrderPoints
pub struct LoyaltyOrderPointsTestData;

impl TestDataGenerator for LoyaltyOrderPointsTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "company_id": Uuid::new_v4().to_string(),
            "loyalty_program_id": Uuid::new_v4().to_string(),
            "customer_id": Uuid::new_v4().to_string(),
            "order_ref_type": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "order_ref_id": Uuid::new_v4().to_string(),
            "coupon_code_id": null,
            "grant_base_amount": 0,
            "granted_points": 0,
            "spent_points": 0,
            "granted_reversed_points": 0,
            "spent_reversed_points": 0,
            "granted_at": now,
            "spent_at": now,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "company_id": Uuid::new_v4().to_string(),
            "loyalty_program_id": Uuid::new_v4().to_string(),
            "customer_id": Uuid::new_v4().to_string(),
            "order_ref_type": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "order_ref_id": Uuid::new_v4().to_string(),
            "coupon_code_id": null,
            "grant_base_amount": 0,
            "granted_points": 0,
            "spent_points": 0,
            "granted_reversed_points": 0,
            "spent_reversed_points": 0,
            "granted_at": now,
            "spent_at": now,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }

    async fn seed_dependencies(&self, api: &ApiTest) -> Vec<(String, String)> {
        let mut deps: Vec<(String, String)> = Vec::new();
        if let Some(id) = super::crud_test_base::create_and_get_id(api, "/api/v1/loyalty_programs", &super::loyalty_program_api_test::LoyaltyProgramTestData).await {
            deps.push(("loyalty_program_id".to_string(), id));
        }
        deps
    }
}

// ============================================================================
// LOYALTYORDERPOINTS API TEST
// ============================================================================

/// LoyaltyOrderPoints API test suite
pub struct LoyaltyOrderPointsApiTest {
    inner: GenericCrudTest<LoyaltyOrderPointsTestData>,
}

impl LoyaltyOrderPointsApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/loyalty_order_points", "LoyaltyOrderPoints");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, LoyaltyOrderPointsTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for LoyaltyOrderPointsApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_loyalty_order_points_crud() {
        let mut test = LoyaltyOrderPointsApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add LoyaltyOrderPoints specific tests here
// <<< CUSTOM TESTS END >>>
