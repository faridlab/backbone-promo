//! Domain events for PromoCampaign entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::PromoCampaign;

/// Domain events for PromoCampaign — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<PromoCampaign>)` variant.
pub type PromoCampaignEvent = CrudEvent<PromoCampaign>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for PromoCampaign events.
pub type PromoCampaignEventPublisher = Arc<dyn CrudEventPublisher<PromoCampaign> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopPromoCampaignEventPublisher = NoOpCrudEventPublisher<PromoCampaign>;
