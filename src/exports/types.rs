//! Public types for Promo module
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! These DTOs are the ONLY types other modules should use.
//! They are decoupled from internal domain entities.

use serde::{Deserialize, Serialize};
use uuid::Uuid;
use chrono::{DateTime, Utc};
use rust_decimal::Decimal;
use crate::domain::entity::*;

// ============================================================================
// COUPONCODE TYPES
// ============================================================================

/// Type-safe ID for CouponCode
///
/// Use this instead of raw Uuid for type safety across modules.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct CouponCodeId(pub Uuid);

impl CouponCodeId {
    pub fn new(id: Uuid) -> Self {
        Self(id)
    }

    pub fn into_inner(self) -> Uuid {
        self.0
    }
}

impl From<Uuid> for CouponCodeId {
    fn from(id: Uuid) -> Self {
        Self(id)
    }
}

impl From<CouponCodeId> for Uuid {
    fn from(id: CouponCodeId) -> Self {
        id.0
    }
}

/// Data transfer object for CouponCode
///
/// This is the public representation of CouponCode for other modules.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CouponCodeDto {
    pub id: CouponCodeId,
    pub company_id: Uuid,
    pub code: String,
    pub pricing_rule_id: Uuid,
    pub description: Option<String>,
    pub max_use: Option<i32>,
    pub used_count: i32,
    pub valid_from: DateTime<Utc>,
    pub valid_upto: Option<DateTime<Utc>>,
    pub is_active: bool,
    pub metadata: serde_json::Value,
}

/// Summary view of CouponCode for list displays
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CouponCodeSummary {
    pub id: CouponCodeId,
}

/// Reference to CouponCode for foreign key relationships
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CouponCodeRef {
    pub id: CouponCodeId,
}

// ============================================================================
// COUPONREDEMPTION TYPES
// ============================================================================

/// Type-safe ID for CouponRedemption
///
/// Use this instead of raw Uuid for type safety across modules.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct CouponRedemptionId(pub Uuid);

impl CouponRedemptionId {
    pub fn new(id: Uuid) -> Self {
        Self(id)
    }

    pub fn into_inner(self) -> Uuid {
        self.0
    }
}

impl From<Uuid> for CouponRedemptionId {
    fn from(id: Uuid) -> Self {
        Self(id)
    }
}

impl From<CouponRedemptionId> for Uuid {
    fn from(id: CouponRedemptionId) -> Self {
        id.0
    }
}

/// Data transfer object for CouponRedemption
///
/// This is the public representation of CouponRedemption for other modules.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CouponRedemptionDto {
    pub id: CouponRedemptionId,
    pub company_id: Uuid,
    pub coupon_id: Uuid,
    pub pricing_rule_id: Uuid,
    pub source_type: String,
    pub source_id: Uuid,
    pub redeemed_at: DateTime<Utc>,
    pub metadata: serde_json::Value,
}

/// Summary view of CouponRedemption for list displays
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CouponRedemptionSummary {
    pub id: CouponRedemptionId,
}

/// Reference to CouponRedemption for foreign key relationships
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CouponRedemptionRef {
    pub id: CouponRedemptionId,
}

// ============================================================================
// LOYALTYPROGRAM TYPES
// ============================================================================

/// Type-safe ID for LoyaltyProgram
///
/// Use this instead of raw Uuid for type safety across modules.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct LoyaltyProgramId(pub Uuid);

impl LoyaltyProgramId {
    pub fn new(id: Uuid) -> Self {
        Self(id)
    }

    pub fn into_inner(self) -> Uuid {
        self.0
    }
}

impl From<Uuid> for LoyaltyProgramId {
    fn from(id: Uuid) -> Self {
        Self(id)
    }
}

impl From<LoyaltyProgramId> for Uuid {
    fn from(id: LoyaltyProgramId) -> Self {
        id.0
    }
}

/// Data transfer object for LoyaltyProgram
///
/// This is the public representation of LoyaltyProgram for other modules.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct LoyaltyProgramDto {
    pub id: LoyaltyProgramId,
    pub company_id: Uuid,
    pub program_name: String,
    pub program_type: LoyaltyProgramType,
    pub collection_factor: Decimal,
    pub conversion_factor: Decimal,
    pub expiry_duration_days: Option<i32>,
    pub from_date: DateTime<Utc>,
    pub to_date: Option<DateTime<Utc>>,
    pub is_active: bool,
    pub metadata: serde_json::Value,
}

/// Summary view of LoyaltyProgram for list displays
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct LoyaltyProgramSummary {
    pub id: LoyaltyProgramId,
    pub program_name: String,
}

/// Reference to LoyaltyProgram for foreign key relationships
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct LoyaltyProgramRef {
    pub id: LoyaltyProgramId,
}

// ============================================================================
// LOYALTYPOINTENTRY TYPES
// ============================================================================

/// Type-safe ID for LoyaltyPointEntry
///
/// Use this instead of raw Uuid for type safety across modules.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct LoyaltyPointEntryId(pub Uuid);

impl LoyaltyPointEntryId {
    pub fn new(id: Uuid) -> Self {
        Self(id)
    }

    pub fn into_inner(self) -> Uuid {
        self.0
    }
}

impl From<Uuid> for LoyaltyPointEntryId {
    fn from(id: Uuid) -> Self {
        Self(id)
    }
}

impl From<LoyaltyPointEntryId> for Uuid {
    fn from(id: LoyaltyPointEntryId) -> Self {
        id.0
    }
}

/// Data transfer object for LoyaltyPointEntry
///
/// This is the public representation of LoyaltyPointEntry for other modules.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct LoyaltyPointEntryDto {
    pub id: LoyaltyPointEntryId,
    pub company_id: Uuid,
    pub loyalty_program_id: Uuid,
    pub customer_id: Uuid,
    pub entry_type: LoyaltyEntryType,
    pub points: Decimal,
    pub purchase_amount: Decimal,
    pub source_type: String,
    pub source_id: Uuid,
    pub posting_date: DateTime<Utc>,
    pub expiry_date: Option<DateTime<Utc>>,
    pub metadata: serde_json::Value,
}

/// Summary view of LoyaltyPointEntry for list displays
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct LoyaltyPointEntrySummary {
    pub id: LoyaltyPointEntryId,
}

/// Reference to LoyaltyPointEntry for foreign key relationships
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct LoyaltyPointEntryRef {
    pub id: LoyaltyPointEntryId,
}

// ============================================================================
// PRICINGRULE TYPES
// ============================================================================

/// Type-safe ID for PricingRule
///
/// Use this instead of raw Uuid for type safety across modules.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct PricingRuleId(pub Uuid);

impl PricingRuleId {
    pub fn new(id: Uuid) -> Self {
        Self(id)
    }

    pub fn into_inner(self) -> Uuid {
        self.0
    }
}

impl From<Uuid> for PricingRuleId {
    fn from(id: Uuid) -> Self {
        Self(id)
    }
}

impl From<PricingRuleId> for Uuid {
    fn from(id: PricingRuleId) -> Self {
        id.0
    }
}

/// Data transfer object for PricingRule
///
/// This is the public representation of PricingRule for other modules.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PricingRuleDto {
    pub id: PricingRuleId,
    pub company_id: Uuid,
    pub title: String,
    pub priority: i32,
    pub scope: RuleScope,
    pub min_order_amount: Decimal,
    pub stackable: bool,
    pub apply_on: ApplyOn,
    pub item_id: Option<Uuid>,
    pub item_group_id: Option<Uuid>,
    pub brand_id: Option<Uuid>,
    pub customer_id: Option<Uuid>,
    pub customer_group_id: Option<Uuid>,
    pub min_qty: Decimal,
    pub max_qty: Option<Decimal>,
    pub min_amount: Decimal,
    pub rate_or_discount: RateOrDiscount,
    pub rate: Option<Decimal>,
    pub discount_percentage: Option<Decimal>,
    pub discount_amount: Option<Decimal>,
    pub currency: String,
    pub valid_from: DateTime<Utc>,
    pub valid_to: Option<DateTime<Utc>>,
    pub coupon_required: bool,
    pub is_active: bool,
    pub metadata: serde_json::Value,
}

/// Summary view of PricingRule for list displays
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PricingRuleSummary {
    pub id: PricingRuleId,
    pub title: String,
}

/// Reference to PricingRule for foreign key relationships
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PricingRuleRef {
    pub id: PricingRuleId,
}

// ============================================================================
// PROMOBUNDLE TYPES
// ============================================================================

/// Type-safe ID for PromoBundle
///
/// Use this instead of raw Uuid for type safety across modules.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct PromoBundleId(pub Uuid);

impl PromoBundleId {
    pub fn new(id: Uuid) -> Self {
        Self(id)
    }

    pub fn into_inner(self) -> Uuid {
        self.0
    }
}

impl From<Uuid> for PromoBundleId {
    fn from(id: Uuid) -> Self {
        Self(id)
    }
}

impl From<PromoBundleId> for Uuid {
    fn from(id: PromoBundleId) -> Self {
        id.0
    }
}

/// Data transfer object for PromoBundle
///
/// This is the public representation of PromoBundle for other modules.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PromoBundleDto {
    pub id: PromoBundleId,
    pub company_id: Uuid,
    pub title: String,
    pub priority: i32,
    pub match_type: BundleMatch,
    pub required_distinct: Option<i32>,
    pub reward: RateOrDiscount,
    pub discount_percentage: Option<Decimal>,
    pub discount_amount: Option<Decimal>,
    pub currency: String,
    pub min_order_amount: Decimal,
    pub stackable: bool,
    pub valid_from: DateTime<Utc>,
    pub valid_to: Option<DateTime<Utc>>,
    pub is_active: bool,
    pub metadata: serde_json::Value,
}

/// Summary view of PromoBundle for list displays
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PromoBundleSummary {
    pub id: PromoBundleId,
    pub title: String,
}

/// Reference to PromoBundle for foreign key relationships
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PromoBundleRef {
    pub id: PromoBundleId,
}

// ============================================================================
// PROMOBUNDLECOMPONENT TYPES
// ============================================================================

/// Type-safe ID for PromoBundleComponent
///
/// Use this instead of raw Uuid for type safety across modules.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct PromoBundleComponentId(pub Uuid);

impl PromoBundleComponentId {
    pub fn new(id: Uuid) -> Self {
        Self(id)
    }

    pub fn into_inner(self) -> Uuid {
        self.0
    }
}

impl From<Uuid> for PromoBundleComponentId {
    fn from(id: Uuid) -> Self {
        Self(id)
    }
}

impl From<PromoBundleComponentId> for Uuid {
    fn from(id: PromoBundleComponentId) -> Self {
        id.0
    }
}

/// Data transfer object for PromoBundleComponent
///
/// This is the public representation of PromoBundleComponent for other modules.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PromoBundleComponentDto {
    pub id: PromoBundleComponentId,
    pub company_id: Uuid,
    pub bundle_id: Uuid,
    pub apply_on: ApplyOn,
    pub item_id: Option<Uuid>,
    pub item_group_id: Option<Uuid>,
    pub brand_id: Option<Uuid>,
    pub min_qty: Decimal,
    pub metadata: serde_json::Value,
}

/// Summary view of PromoBundleComponent for list displays
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PromoBundleComponentSummary {
    pub id: PromoBundleComponentId,
}

/// Reference to PromoBundleComponent for foreign key relationships
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PromoBundleComponentRef {
    pub id: PromoBundleComponentId,
}

// ============================================================================
// CUSTOM TYPES
// ============================================================================

// <<< CUSTOM TYPES START >>>
// Add custom public types here
// <<< CUSTOM TYPES END >>>
