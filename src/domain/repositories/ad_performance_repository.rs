//! Repository trait for AdPerformance aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the AdPerformance aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{AdPerformance, AdPlacement, AdStatus, AdType};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct AdPerformancePaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl AdPerformancePaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct AdPerformancePaginatedResult {
    pub data: Vec<AdPerformance>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct AdPerformanceFilter {
    pub advertisement_id: Option<Uuid>,
    pub provider_id: Option<Uuid>,
    pub ad_type: Option<AdType>,
    pub placement: Option<AdPlacement>,
    pub status: Option<AdStatus>,
    pub currency: Option<String>,
}

impl AdPerformanceFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.advertisement_id.is_some() || self.provider_id.is_some() || self.ad_type.is_some() || self.placement.is_some() || self.status.is_some() || self.currency.is_some()
    }
}

/// Repository trait for AdPerformance aggregate
///
/// This trait defines the persistence contract for AdPerformance entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait AdPerformanceRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new ad_performance entity
    async fn save(&self, entity: &AdPerformance) -> Result<AdPerformance>;

    /// Find ad_performance by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<AdPerformance>>;

    /// Find all ad_performance entities
    async fn find_all(&self) -> Result<Vec<AdPerformance>>;

    /// Update ad_performance by ID
    async fn update(&self, id: &str, entity: &AdPerformance) -> Result<Option<AdPerformance>>;

    /// Delete ad_performance by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List ad_performance with pagination
    async fn list(&self, params: AdPerformancePaginationParams) -> Result<AdPerformancePaginatedResult>;

    /// List ad_performance with pagination and filters
    async fn list_with_filters(&self, params: AdPerformancePaginationParams, filters: AdPerformanceFilter) -> Result<AdPerformancePaginatedResult>;

    /// Count all ad_performance entities
    async fn count(&self) -> Result<u64>;

    /// Count ad_performance entities matching filters
    async fn count_with_filters(&self, filters: AdPerformanceFilter) -> Result<u64>;

    /// Check if ad_performance exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete ad_performance by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted ad_performance by ID
    async fn restore(&self, id: &str) -> Result<Option<AdPerformance>>;

    /// List soft-deleted ad_performance entities
    async fn list_deleted(&self, params: AdPerformancePaginationParams) -> Result<AdPerformancePaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save ad_performance entities
    async fn bulk_save(&self, entities: &[AdPerformance]) -> Result<Vec<AdPerformance>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
