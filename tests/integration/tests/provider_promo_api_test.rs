//! ProviderPromo API Integration Tests
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Tests the ProviderPromo CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::helpers::CommonUtils;

// ============================================================================
// PROVIDERPROMO TEST DATA GENERATOR
// ============================================================================

/// Test data generator for ProviderPromo
pub struct ProviderPromoTestData;

impl TestDataGenerator for ProviderPromoTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "provider_id": null,
            "outlet_id": null,
            "promo_name": null,
            "promo_code": null,
            "description": "Test description",
            "promo_type": null,
            "discount_value": null,
            "max_discount": null,
            "currency": null,
            "min_order_amount": null,
            "min_weight_kg": null,
            "applicable_services": null,
            "applicable_categories": null,
            "valid_from": null,
            "valid_until": null,
            "usage_limit": null,
            "usage_limit_per_customer": null,
            "usage_count": null,
            "eligible_customers": null,
            "eligible_tiers": null,
            "is_active": true,
            "is_public": false,
            "is_stackable": false,
            "banner_url": null,
            "terms_conditions": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "provider_id": null,
            "outlet_id": null,
            "promo_name": null,
            "promo_code": null,
            "description": "Test description",
            "promo_type": null,
            "discount_value": null,
            "max_discount": null,
            "currency": null,
            "min_order_amount": null,
            "min_weight_kg": null,
            "applicable_services": null,
            "applicable_categories": null,
            "valid_from": null,
            "valid_until": null,
            "usage_limit": null,
            "usage_limit_per_customer": null,
            "usage_count": null,
            "eligible_customers": null,
            "eligible_tiers": null,
            "is_active": true,
            "is_public": false,
            "is_stackable": false,
            "banner_url": null,
            "terms_conditions": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// PROVIDERPROMO API TEST
// ============================================================================

/// ProviderPromo API test suite
pub struct ProviderPromoApiTest {
    inner: GenericCrudTest<ProviderPromoTestData>,
}

impl ProviderPromoApiTest {
    pub fn new() -> Self {
        let config = CrudTestConfig::new("/api/v1/provider_promos", "ProviderPromo");
        Self {
            inner: GenericCrudTest::new(config, ProviderPromoTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for ProviderPromoApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_provider_promo_crud() {
        let mut test = ProviderPromoApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add ProviderPromo specific tests here
// <<< CUSTOM TESTS END >>>
