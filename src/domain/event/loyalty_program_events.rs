//! Domain events for LoyaltyProgram entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::LoyaltyProgram;

/// Domain events for LoyaltyProgram — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<LoyaltyProgram>)` variant.
pub type LoyaltyProgramEvent = CrudEvent<LoyaltyProgram>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for LoyaltyProgram events.
pub type LoyaltyProgramEventPublisher = Arc<dyn CrudEventPublisher<LoyaltyProgram> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopLoyaltyProgramEventPublisher = NoOpCrudEventPublisher<LoyaltyProgram>;
