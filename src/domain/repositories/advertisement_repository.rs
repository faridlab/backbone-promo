//! Repository trait for Advertisement aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the Advertisement aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{Advertisement, AdBillingModel, AdPlacement, AdStatus};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct AdvertisementPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl AdvertisementPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct AdvertisementPaginatedResult {
    pub data: Vec<Advertisement>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct AdvertisementFilter {
    pub name: Option<String>,
    pub ad_number: Option<String>,
    pub provider_id: Option<Uuid>,
    pub placement: Option<AdPlacement>,
    pub headline: Option<String>,
    pub description: Option<String>,
    pub image_url: Option<String>,
    pub image_url_mobile: Option<String>,
    pub click_url: Option<String>,
    pub action_type: Option<String>,
    pub action_reference: Option<String>,
    pub target_audience: Option<String>,
    pub status: Option<AdStatus>,
    pub currency: Option<String>,
    pub billing_model: Option<AdBillingModel>,
    pub approved_by: Option<Uuid>,
    pub rejection_reason: Option<String>,
}

impl AdvertisementFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.name.is_some() || self.ad_number.is_some() || self.provider_id.is_some() || self.placement.is_some() || self.headline.is_some() || self.description.is_some() || self.image_url.is_some() || self.image_url_mobile.is_some() || self.click_url.is_some() || self.action_type.is_some() || self.action_reference.is_some() || self.target_audience.is_some() || self.status.is_some() || self.currency.is_some() || self.billing_model.is_some() || self.approved_by.is_some() || self.rejection_reason.is_some()
    }
}

/// Repository trait for Advertisement aggregate
///
/// This trait defines the persistence contract for Advertisement entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait AdvertisementRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new advertisement entity
    async fn save(&self, entity: &Advertisement) -> Result<Advertisement>;

    /// Find advertisement by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<Advertisement>>;

    /// Find all advertisement entities
    async fn find_all(&self) -> Result<Vec<Advertisement>>;

    /// Update advertisement by ID
    async fn update(&self, id: &str, entity: &Advertisement) -> Result<Option<Advertisement>>;

    /// Delete advertisement by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List advertisement with pagination
    async fn list(&self, params: AdvertisementPaginationParams) -> Result<AdvertisementPaginatedResult>;

    /// List advertisement with pagination and filters
    async fn list_with_filters(&self, params: AdvertisementPaginationParams, filters: AdvertisementFilter) -> Result<AdvertisementPaginatedResult>;

    /// Count all advertisement entities
    async fn count(&self) -> Result<u64>;

    /// Count advertisement entities matching filters
    async fn count_with_filters(&self, filters: AdvertisementFilter) -> Result<u64>;

    /// Check if advertisement exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete advertisement by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted advertisement by ID
    async fn restore(&self, id: &str) -> Result<Option<Advertisement>>;

    /// List soft-deleted advertisement entities
    async fn list_deleted(&self, params: AdvertisementPaginationParams) -> Result<AdvertisementPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Query by Unique Fields
    // =========================================================================

    /// Find advertisement by ad_number
    async fn find_by_ad_number(&self, ad_number: &str) -> Result<Option<Advertisement>>;

    // =========================================================================
    // Existence Checks by Unique Fields
    // =========================================================================

    /// Check if advertisement exists by ad_number
    async fn exists_by_ad_number(&self, ad_number: &str) -> Result<bool>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save advertisement entities
    async fn bulk_save(&self, entities: &[Advertisement]) -> Result<Vec<Advertisement>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
