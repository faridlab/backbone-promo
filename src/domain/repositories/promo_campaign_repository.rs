//! Repository trait for PromoCampaign aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the PromoCampaign aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;

use crate::domain::entity::{PromoCampaign, CampaignFunder, CampaignPromoType, CampaignStatus, CampaignTarget};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PromoCampaignPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl PromoCampaignPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct PromoCampaignPaginatedResult {
    pub data: Vec<PromoCampaign>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PromoCampaignFilter {
    pub name: Option<String>,
    pub code: Option<String>,
    pub description: Option<String>,
    pub promo_type: Option<CampaignPromoType>,
    pub target_audience: Option<CampaignTarget>,
    pub first_order_only: Option<bool>,
    pub status: Option<CampaignStatus>,
    pub funded_by: Option<CampaignFunder>,
    pub display_on_app: Option<bool>,
    pub banner_image_url: Option<String>,
    pub terms_and_conditions: Option<String>,
}

impl PromoCampaignFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.name.is_some() || self.code.is_some() || self.description.is_some() || self.promo_type.is_some() || self.target_audience.is_some() || self.first_order_only.is_some() || self.status.is_some() || self.funded_by.is_some() || self.display_on_app.is_some() || self.banner_image_url.is_some() || self.terms_and_conditions.is_some()
    }
}

/// Repository trait for PromoCampaign aggregate
///
/// This trait defines the persistence contract for PromoCampaign entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait PromoCampaignRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new promo_campaign entity
    async fn save(&self, entity: &PromoCampaign) -> Result<PromoCampaign>;

    /// Find promo_campaign by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<PromoCampaign>>;

    /// Find all promo_campaign entities
    async fn find_all(&self) -> Result<Vec<PromoCampaign>>;

    /// Update promo_campaign by ID
    async fn update(&self, id: &str, entity: &PromoCampaign) -> Result<Option<PromoCampaign>>;

    /// Delete promo_campaign by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List promo_campaign with pagination
    async fn list(&self, params: PromoCampaignPaginationParams) -> Result<PromoCampaignPaginatedResult>;

    /// List promo_campaign with pagination and filters
    async fn list_with_filters(&self, params: PromoCampaignPaginationParams, filters: PromoCampaignFilter) -> Result<PromoCampaignPaginatedResult>;

    /// Count all promo_campaign entities
    async fn count(&self) -> Result<u64>;

    /// Count promo_campaign entities matching filters
    async fn count_with_filters(&self, filters: PromoCampaignFilter) -> Result<u64>;

    /// Check if promo_campaign exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete promo_campaign by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted promo_campaign by ID
    async fn restore(&self, id: &str) -> Result<Option<PromoCampaign>>;

    /// List soft-deleted promo_campaign entities
    async fn list_deleted(&self, params: PromoCampaignPaginationParams) -> Result<PromoCampaignPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Query by Unique Fields
    // =========================================================================

    /// Find promo_campaign by code
    async fn find_by_code(&self, code: &str) -> Result<Option<PromoCampaign>>;

    // =========================================================================
    // Existence Checks by Unique Fields
    // =========================================================================

    /// Check if promo_campaign exists by code
    async fn exists_by_code(&self, code: &str) -> Result<bool>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save promo_campaign entities
    async fn bulk_save(&self, entities: &[PromoCampaign]) -> Result<Vec<PromoCampaign>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
