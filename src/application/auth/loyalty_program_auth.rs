//! Auth configuration for LoyaltyProgram handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<LoyaltyProgram>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::LoyaltyProgramService;
use crate::domain::entity::LoyaltyProgram;

/// Permission string constants for LoyaltyProgram operations.
pub mod permissions {
    pub const LIST:        &str = "loyalty_program:list";
    pub const READ:        &str = "loyalty_program:read";
    pub const CREATE:      &str = "loyalty_program:create";
    pub const UPDATE:      &str = "loyalty_program:update";
    pub const DELETE:      &str = "loyalty_program:delete";
    pub const RESTORE:     &str = "loyalty_program:restore";
    pub const TRASH:       &str = "loyalty_program:trash";
    pub const EMPTY_TRASH: &str = "loyalty_program:empty_trash";
    pub const BULK_CREATE: &str = "loyalty_program:bulk_create";
    pub const UPSERT:      &str = "loyalty_program:upsert";
}

/// Resource policy for LoyaltyProgram — maps CRUD actions to permission strings.
pub struct LoyaltyProgramPolicy;

#[async_trait]
impl ResourcePolicy<LoyaltyProgram> for LoyaltyProgramPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &LoyaltyProgram,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for LoyaltyProgram handlers.
///
/// Wraps `LoyaltyProgramService` and checks `LoyaltyProgramPolicy` before delegating.
pub type LoyaltyProgramGuard = PermissionGuard<LoyaltyProgram>;

/// Service-integrated permission guard for LoyaltyProgram (for use in generated handlers).
pub type LoyaltyProgramServiceGuard = ServicePermissionGuard<LoyaltyProgram, LoyaltyProgramService, LoyaltyProgramPolicy>;

// <<< CUSTOM
// END CUSTOM
