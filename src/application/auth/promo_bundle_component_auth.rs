//! Auth configuration for PromoBundleComponent handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<PromoBundleComponent>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::PromoBundleComponentService;
use crate::domain::entity::PromoBundleComponent;

/// Permission string constants for PromoBundleComponent operations.
pub mod permissions {
    pub const LIST:        &str = "promo_bundle_component:list";
    pub const READ:        &str = "promo_bundle_component:read";
    pub const CREATE:      &str = "promo_bundle_component:create";
    pub const UPDATE:      &str = "promo_bundle_component:update";
    pub const DELETE:      &str = "promo_bundle_component:delete";
    pub const RESTORE:     &str = "promo_bundle_component:restore";
    pub const TRASH:       &str = "promo_bundle_component:trash";
    pub const EMPTY_TRASH: &str = "promo_bundle_component:empty_trash";
    pub const BULK_CREATE: &str = "promo_bundle_component:bulk_create";
    pub const UPSERT:      &str = "promo_bundle_component:upsert";
}

/// Resource policy for PromoBundleComponent — maps CRUD actions to permission strings.
pub struct PromoBundleComponentPolicy;

#[async_trait]
impl ResourcePolicy<PromoBundleComponent> for PromoBundleComponentPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &PromoBundleComponent,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for PromoBundleComponent handlers.
///
/// Wraps `PromoBundleComponentService` and checks `PromoBundleComponentPolicy` before delegating.
pub type PromoBundleComponentGuard = PermissionGuard<PromoBundleComponent>;

/// Service-integrated permission guard for PromoBundleComponent (for use in generated handlers).
pub type PromoBundleComponentServiceGuard = ServicePermissionGuard<PromoBundleComponent, PromoBundleComponentService, PromoBundleComponentPolicy>;

// <<< CUSTOM
// END CUSTOM
