//! LoyaltyProgram seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial LoyaltyProgram data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// LOYALTYPROGRAM SEEDER
// ============================================================================

/// Seeder for loyalty_program
pub struct SeedLoyaltyProgramSeeder;

impl SeedLoyaltyProgramSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedLoyaltyProgramSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedLoyaltyProgramSeeder {
    fn name(&self) -> &'static str {
        "SeedLoyaltyProgramSeeder"
    }

    fn order(&self) -> i32 {
        3
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if promo.loyalty_programs table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM promo.loyalty_programs")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/loyalty_program_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM promo.loyalty_programs")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

