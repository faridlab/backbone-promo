//! Repository trait for LoyaltyProgram aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the LoyaltyProgram aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{LoyaltyProgram, LoyaltyProgramType};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct LoyaltyProgramPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl LoyaltyProgramPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct LoyaltyProgramPaginatedResult {
    pub data: Vec<LoyaltyProgram>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct LoyaltyProgramFilter {
    pub company_id: Option<Uuid>,
    pub program_name: Option<String>,
    pub program_type: Option<LoyaltyProgramType>,
    pub is_active: Option<bool>,
}

impl LoyaltyProgramFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.company_id.is_some() || self.program_name.is_some() || self.program_type.is_some() || self.is_active.is_some()
    }
}

/// Repository trait for LoyaltyProgram aggregate
///
/// This trait defines the persistence contract for LoyaltyProgram entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait LoyaltyProgramRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new loyalty_program entity
    async fn save(&self, entity: &LoyaltyProgram) -> Result<LoyaltyProgram>;

    /// Find loyalty_program by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<LoyaltyProgram>>;

    /// Find all loyalty_program entities
    async fn find_all(&self) -> Result<Vec<LoyaltyProgram>>;

    /// Update loyalty_program by ID
    async fn update(&self, id: &str, entity: &LoyaltyProgram) -> Result<Option<LoyaltyProgram>>;

    /// Delete loyalty_program by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List loyalty_program with pagination
    async fn list(&self, params: LoyaltyProgramPaginationParams) -> Result<LoyaltyProgramPaginatedResult>;

    /// List loyalty_program with pagination and filters
    async fn list_with_filters(&self, params: LoyaltyProgramPaginationParams, filters: LoyaltyProgramFilter) -> Result<LoyaltyProgramPaginatedResult>;

    /// Count all loyalty_program entities
    async fn count(&self) -> Result<u64>;

    /// Count loyalty_program entities matching filters
    async fn count_with_filters(&self, filters: LoyaltyProgramFilter) -> Result<u64>;

    /// Check if loyalty_program exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete loyalty_program by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted loyalty_program by ID
    async fn restore(&self, id: &str) -> Result<Option<LoyaltyProgram>>;

    /// List soft-deleted loyalty_program entities
    async fn list_deleted(&self, params: LoyaltyProgramPaginationParams) -> Result<LoyaltyProgramPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save loyalty_program entities
    async fn bulk_save(&self, entities: &[LoyaltyProgram]) -> Result<Vec<LoyaltyProgram>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
