//! Domain events for PricingRule entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::PricingRule;

/// Domain events for PricingRule — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<PricingRule>)` variant.
pub type PricingRuleEvent = CrudEvent<PricingRule>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for PricingRule events.
pub type PricingRuleEventPublisher = Arc<dyn CrudEventPublisher<PricingRule> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopPricingRuleEventPublisher = NoOpCrudEventPublisher<PricingRule>;
