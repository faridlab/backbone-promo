//! Auth configuration for Advertisement handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<Advertisement>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::AdvertisementService;
use crate::domain::entity::Advertisement;

/// Permission string constants for Advertisement operations.
pub mod permissions {
    pub const LIST:        &str = "advertisement:list";
    pub const READ:        &str = "advertisement:read";
    pub const CREATE:      &str = "advertisement:create";
    pub const UPDATE:      &str = "advertisement:update";
    pub const DELETE:      &str = "advertisement:delete";
    pub const RESTORE:     &str = "advertisement:restore";
    pub const TRASH:       &str = "advertisement:trash";
    pub const EMPTY_TRASH: &str = "advertisement:empty_trash";
    pub const BULK_CREATE: &str = "advertisement:bulk_create";
    pub const UPSERT:      &str = "advertisement:upsert";
}

/// Resource policy for Advertisement — maps CRUD actions to permission strings.
pub struct AdvertisementPolicy;

#[async_trait]
impl ResourcePolicy<Advertisement> for AdvertisementPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &Advertisement,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for Advertisement handlers.
///
/// Wraps `AdvertisementService` and checks `AdvertisementPolicy` before delegating.
pub type AdvertisementGuard = PermissionGuard<Advertisement, AdvertisementService, AdvertisementPolicy>;

/// Service-integrated permission guard for Advertisement (for use in generated handlers).
pub type AdvertisementServiceGuard = ServicePermissionGuard<Advertisement, AdvertisementService, AdvertisementPolicy>;

// <<< CUSTOM
// END CUSTOM
