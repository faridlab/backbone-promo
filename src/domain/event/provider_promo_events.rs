//! Domain events for ProviderPromo entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::ProviderPromo;

/// Domain events for ProviderPromo — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<ProviderPromo>)` variant.
pub type ProviderPromoEvent = CrudEvent<ProviderPromo>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for ProviderPromo events.
pub type ProviderPromoEventPublisher = Arc<dyn CrudEventPublisher<ProviderPromo> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopProviderPromoEventPublisher = NoOpCrudEventPublisher<ProviderPromo>;
