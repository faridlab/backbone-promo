//! Auth configuration for CouponRedemption handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<CouponRedemption>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::CouponRedemptionService;
use crate::domain::entity::CouponRedemption;

/// Permission string constants for CouponRedemption operations.
pub mod permissions {
    pub const LIST:        &str = "coupon_redemption:list";
    pub const READ:        &str = "coupon_redemption:read";
    pub const CREATE:      &str = "coupon_redemption:create";
    pub const UPDATE:      &str = "coupon_redemption:update";
    pub const DELETE:      &str = "coupon_redemption:delete";
    pub const RESTORE:     &str = "coupon_redemption:restore";
    pub const TRASH:       &str = "coupon_redemption:trash";
    pub const EMPTY_TRASH: &str = "coupon_redemption:empty_trash";
    pub const BULK_CREATE: &str = "coupon_redemption:bulk_create";
    pub const UPSERT:      &str = "coupon_redemption:upsert";
}

/// Resource policy for CouponRedemption — maps CRUD actions to permission strings.
pub struct CouponRedemptionPolicy;

#[async_trait]
impl ResourcePolicy<CouponRedemption> for CouponRedemptionPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &CouponRedemption,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for CouponRedemption handlers.
///
/// Wraps `CouponRedemptionService` and checks `CouponRedemptionPolicy` before delegating.
pub type CouponRedemptionGuard = PermissionGuard<CouponRedemption>;

/// Service-integrated permission guard for CouponRedemption (for use in generated handlers).
pub type CouponRedemptionServiceGuard = ServicePermissionGuard<CouponRedemption, CouponRedemptionService, CouponRedemptionPolicy>;

// <<< CUSTOM
// END CUSTOM
