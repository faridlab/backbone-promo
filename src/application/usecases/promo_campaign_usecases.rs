//! Use case type aliases for PromoCampaign aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Each alias delegates to the matching generic use case in backbone-core.
//! Custom pre/post hooks go in `UseCaseHooks` implementations (// <<< CUSTOM).

use backbone_core::{
    CreateUseCase, UpdateUseCase, GetUseCase, DeleteUseCase, ListUseCase,
};

use crate::presentation::dto::{CreatePromoCampaignDto, UpdatePromoCampaignDto};
use crate::application::service::PromoCampaignService;
use crate::domain::entity::PromoCampaign;

// ─── Use case type aliases ───────────────────────────────────────────────────

pub type CreatePromoCampaignUseCase = CreateUseCase<PromoCampaign, CreatePromoCampaignDto, PromoCampaignService>;
pub type UpdatePromoCampaignUseCase = UpdateUseCase<PromoCampaign, UpdatePromoCampaignDto, PromoCampaignService>;
pub type GetPromoCampaignUseCase    = GetUseCase<PromoCampaign, PromoCampaignService>;
pub type DeletePromoCampaignUseCase = DeleteUseCase<PromoCampaign, PromoCampaignService>;
pub type ListPromoCampaignUseCase   = ListUseCase<PromoCampaign, PromoCampaignService>;

// <<< CUSTOM
// END CUSTOM
