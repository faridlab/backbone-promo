//! Repository trait for LoyaltyPointEntry aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the LoyaltyPointEntry aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{LoyaltyPointEntry, LoyaltyEntryType};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct LoyaltyPointEntryPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl LoyaltyPointEntryPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct LoyaltyPointEntryPaginatedResult {
    pub data: Vec<LoyaltyPointEntry>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct LoyaltyPointEntryFilter {
    pub company_id: Option<Uuid>,
    pub loyalty_program_id: Option<Uuid>,
    pub customer_id: Option<Uuid>,
    pub entry_type: Option<LoyaltyEntryType>,
    pub source_type: Option<String>,
    pub source_id: Option<Uuid>,
}

impl LoyaltyPointEntryFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.company_id.is_some() || self.loyalty_program_id.is_some() || self.customer_id.is_some() || self.entry_type.is_some() || self.source_type.is_some() || self.source_id.is_some()
    }
}

/// Repository trait for LoyaltyPointEntry aggregate
///
/// This trait defines the persistence contract for LoyaltyPointEntry entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait LoyaltyPointEntryRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new loyalty_point_entry entity
    async fn save(&self, entity: &LoyaltyPointEntry) -> Result<LoyaltyPointEntry>;

    /// Find loyalty_point_entry by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<LoyaltyPointEntry>>;

    /// Find all loyalty_point_entry entities
    async fn find_all(&self) -> Result<Vec<LoyaltyPointEntry>>;

    /// Update loyalty_point_entry by ID
    async fn update(&self, id: &str, entity: &LoyaltyPointEntry) -> Result<Option<LoyaltyPointEntry>>;

    /// Delete loyalty_point_entry by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List loyalty_point_entry with pagination
    async fn list(&self, params: LoyaltyPointEntryPaginationParams) -> Result<LoyaltyPointEntryPaginatedResult>;

    /// List loyalty_point_entry with pagination and filters
    async fn list_with_filters(&self, params: LoyaltyPointEntryPaginationParams, filters: LoyaltyPointEntryFilter) -> Result<LoyaltyPointEntryPaginatedResult>;

    /// Count all loyalty_point_entry entities
    async fn count(&self) -> Result<u64>;

    /// Count loyalty_point_entry entities matching filters
    async fn count_with_filters(&self, filters: LoyaltyPointEntryFilter) -> Result<u64>;

    /// Check if loyalty_point_entry exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete loyalty_point_entry by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted loyalty_point_entry by ID
    async fn restore(&self, id: &str) -> Result<Option<LoyaltyPointEntry>>;

    /// List soft-deleted loyalty_point_entry entities
    async fn list_deleted(&self, params: LoyaltyPointEntryPaginationParams) -> Result<LoyaltyPointEntryPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save loyalty_point_entry entities
    async fn bulk_save(&self, entities: &[LoyaltyPointEntry]) -> Result<Vec<LoyaltyPointEntry>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
