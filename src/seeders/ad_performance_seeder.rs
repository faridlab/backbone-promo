//! AdPerformance seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial AdPerformance data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// ADPERFORMANCE SEEDER
// ============================================================================

/// Seeder for ad_performance
pub struct SeedAdPerformanceSeeder;

impl SeedAdPerformanceSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedAdPerformanceSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedAdPerformanceSeeder {
    fn name(&self) -> &'static str {
        "SeedAdPerformanceSeeder"
    }

    fn order(&self) -> i32 {
        1
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if ad_performances table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM ad_performances")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/ad_performance_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM ad_performances")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

