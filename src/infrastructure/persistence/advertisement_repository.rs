//! Repository for Advertisement entities
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<Advertisement, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;

use crate::domain::entity::Advertisement;

/// Table name for Advertisement entities
pub const TABLE_NAME: &str = "advertisements";

/// Repository for Advertisement entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct AdvertisementRepository(
    backbone_orm::GenericCrudRepository<Advertisement, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for AdvertisementRepository {
    type Target = backbone_orm::GenericCrudRepository<Advertisement, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl AdvertisementRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

impl AdvertisementRepository {
    /// Find advertisement by ad_number (excludes soft-deleted)
    pub async fn find_by_ad_number(&self, ad_number: &str) -> Result<Option<Advertisement>> {
        self.find_by_text_field("ad_number", ad_number).await
    }

    /// Check if advertisement exists by ad_number (excludes soft-deleted)
    pub async fn exists_by_ad_number(&self, ad_number: &str) -> Result<bool> {
        self.exists_by_text_field("ad_number", ad_number).await
    }

}

backbone_core::impl_crud_repository!(AdvertisementRepository, Advertisement, soft_delete);
