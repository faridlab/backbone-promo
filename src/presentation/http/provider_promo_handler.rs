//! ProviderPromo REST handlers
//!
//! Auto-generated by backbone-schema. Do not edit manually.
//!
//! Uses Axum and backbone-core's BackboneCrudHandler for all 12 CRUD endpoints.

use std::collections::HashMap;
use std::sync::Arc;

use axum::Router;
use serde::{Deserialize, Serialize};
use uuid::Uuid;
use chrono::{DateTime, Utc};
use rust_decimal::Decimal;

// Backbone framework imports
use backbone_core::http::BackboneCrudHandler;

// Auth integration (optional)
#[cfg(feature = "auth")]
use backbone_auth::{AuthContext, AuthMiddleware};

// Domain imports
use crate::domain::entity::*;
use crate::application::service::{ProviderPromoService, ServiceError};

// DTO imports
use crate::presentation::dto::{CreateProviderPromoDto, UpdateProviderPromoDto, PatchProviderPromoDto, ProviderPromoResponseDto};


/// Application error type
#[derive(Debug, thiserror::Error)]
pub enum ProviderPromoError {
    #[error("Not found: {0}")]
    NotFound(String),
    #[error("Validation error: {0}")]
    Validation(String),
    #[error("Database error: {0}")]
    Database(String),
    #[error("Internal error: {0}")]
    Internal(String),
}

impl From<ServiceError> for ProviderPromoError {
    fn from(err: ServiceError) -> Self {
        match err {
            ServiceError::NotFound => Self::NotFound(err.to_string()),
            ServiceError::Validation(ref msg) => Self::Validation(msg.clone()),
            ServiceError::AlreadyExists(ref msg) => Self::Validation(msg.clone()),
            ServiceError::Repository(ref e) => Self::Database(e.to_string()),
            ServiceError::Internal(ref msg) => Self::Internal(msg.clone()),
        }
    }
}

impl axum::response::IntoResponse for ProviderPromoError {
    fn into_response(self) -> axum::response::Response {
        use axum::http::StatusCode;
        use axum::Json;

        let (status, code) = match &self {
            Self::NotFound(_) => (StatusCode::NOT_FOUND, "PROVIDERPROMO_NOT_FOUND"),
            Self::Validation(_) => (StatusCode::BAD_REQUEST, "PROVIDERPROMO_VALIDATION_ERROR"),
            Self::Database(_) => (StatusCode::INTERNAL_SERVER_ERROR, "PROVIDERPROMO_DATABASE_ERROR"),
            Self::Internal(_) => (StatusCode::INTERNAL_SERVER_ERROR, "PROVIDERPROMO_INTERNAL_ERROR"),
        };

        let body = serde_json::json!({
            "success": false,
            "error": code,
            "message": self.to_string(),
        });

        (status, Json(body)).into_response()
    }
}

// =============================================================================
// Route Configuration
// =============================================================================

/// Create Axum router with all 16 Backbone endpoints for ProviderPromo.
///
/// # Routes
///
/// | Method | Path | Description |
/// |--------|------|-------------|
/// | GET | /provider_promos | List with pagination |
/// | POST | /provider_promos | Create new |
/// | GET | /provider_promos/:id | Get by ID |
/// | PUT | /provider_promos/:id | Full update |
/// | PATCH | /provider_promos/:id | Partial update |
/// | DELETE | /provider_promos/:id | Soft delete |
/// | POST | /provider_promos/bulk | Bulk create |
/// | POST | /provider_promos/upsert | Upsert |
/// | GET | /provider_promos/trash | List deleted |
/// | POST | /provider_promos/:id/restore | Restore |
/// | DELETE | /provider_promos/empty | Empty trash |
/// | GET | /provider_promos/:id/deleted | Get deleted by ID |
/// | DELETE | /provider_promos/trash/:id | Permanent delete from trash |
/// | GET | /provider_promos/count | Count active entities |
/// | GET | /provider_promos/trash/count | Count deleted entities |
///
/// # Example
///
/// ```text
/// let service = Arc::new(ProviderPromoService::with_repository(repository));
/// let router = create_provider_promo_routes(service);
/// ```
pub fn create_provider_promo_routes(service: Arc<ProviderPromoService>) -> Router {
    BackboneCrudHandler::<ProviderPromoService, ProviderPromo, CreateProviderPromoDto, UpdateProviderPromoDto, ProviderPromoResponseDto>::routes(
        service,
        "/provider_promos",
    )
}

/// Create authenticated routes with auth middleware.
///
/// Requires the `auth` feature flag. The `AuthMiddleware` implementation
/// is responsible for extracting and validating tokens, then providing
/// an `AuthContext` via request extensions.
#[cfg(feature = "auth")]
pub fn create_protected_provider_promo_routes<A: AuthMiddleware + 'static>(
    service: Arc<ProviderPromoService>,
    auth: Arc<A>,
) -> Router {
    use axum::middleware;
    use axum::response::IntoResponse;

    let auth_layer = auth.clone();
    create_provider_promo_routes(service)
        .layer(middleware::from_fn(move |mut req: axum::extract::Request, next: axum::middleware::Next| {
            let auth = auth_layer.clone();
            async move {
                match auth.authenticate(&req).await {
                    Ok(ctx) => {
                        req.extensions_mut().insert(ctx);
                        next.run(req).await
                    }
                    Err(_) => {
                        (axum::http::StatusCode::UNAUTHORIZED,
                         axum::Json(serde_json::json!({
                             "success": false,
                             "error": "unauthorized",
                             "message": "Authentication required"
                         }))
                        ).into_response()
                    }
                }
            }
        }))
}

