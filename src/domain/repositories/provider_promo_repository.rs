//! Repository trait for ProviderPromo aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the ProviderPromo aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{ProviderPromo, PromoEligibility, PromoType};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct ProviderPromoPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl ProviderPromoPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct ProviderPromoPaginatedResult {
    pub data: Vec<ProviderPromo>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct ProviderPromoFilter {
    pub provider_id: Option<Uuid>,
    pub outlet_id: Option<Uuid>,
    pub promo_name: Option<String>,
    pub promo_code: Option<String>,
    pub description: Option<String>,
    pub promo_type: Option<PromoType>,
    pub currency: Option<String>,
    pub eligible_customers: Option<PromoEligibility>,
    pub is_active: Option<bool>,
    pub is_public: Option<bool>,
    pub is_stackable: Option<bool>,
    pub banner_url: Option<String>,
    pub terms_conditions: Option<String>,
}

impl ProviderPromoFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.provider_id.is_some() || self.outlet_id.is_some() || self.promo_name.is_some() || self.promo_code.is_some() || self.description.is_some() || self.promo_type.is_some() || self.currency.is_some() || self.eligible_customers.is_some() || self.is_active.is_some() || self.is_public.is_some() || self.is_stackable.is_some() || self.banner_url.is_some() || self.terms_conditions.is_some()
    }
}

/// Repository trait for ProviderPromo aggregate
///
/// This trait defines the persistence contract for ProviderPromo entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait ProviderPromoRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new provider_promo entity
    async fn save(&self, entity: &ProviderPromo) -> Result<ProviderPromo>;

    /// Find provider_promo by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<ProviderPromo>>;

    /// Find all provider_promo entities
    async fn find_all(&self) -> Result<Vec<ProviderPromo>>;

    /// Update provider_promo by ID
    async fn update(&self, id: &str, entity: &ProviderPromo) -> Result<Option<ProviderPromo>>;

    /// Delete provider_promo by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List provider_promo with pagination
    async fn list(&self, params: ProviderPromoPaginationParams) -> Result<ProviderPromoPaginatedResult>;

    /// List provider_promo with pagination and filters
    async fn list_with_filters(&self, params: ProviderPromoPaginationParams, filters: ProviderPromoFilter) -> Result<ProviderPromoPaginatedResult>;

    /// Count all provider_promo entities
    async fn count(&self) -> Result<u64>;

    /// Count provider_promo entities matching filters
    async fn count_with_filters(&self, filters: ProviderPromoFilter) -> Result<u64>;

    /// Check if provider_promo exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete provider_promo by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted provider_promo by ID
    async fn restore(&self, id: &str) -> Result<Option<ProviderPromo>>;

    /// List soft-deleted provider_promo entities
    async fn list_deleted(&self, params: ProviderPromoPaginationParams) -> Result<ProviderPromoPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save provider_promo entities
    async fn bulk_save(&self, entities: &[ProviderPromo]) -> Result<Vec<ProviderPromo>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
