//! Repository trait for PromoBundle aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the PromoBundle aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{PromoBundle, BundleMatch, RateOrDiscount};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PromoBundlePaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl PromoBundlePaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct PromoBundlePaginatedResult {
    pub data: Vec<PromoBundle>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PromoBundleFilter {
    pub company_id: Option<Uuid>,
    pub title: Option<String>,
    pub match_type: Option<BundleMatch>,
    pub reward: Option<RateOrDiscount>,
    pub reward_item_id: Option<Uuid>,
    pub currency: Option<String>,
    pub stackable: Option<bool>,
    pub is_active: Option<bool>,
}

impl PromoBundleFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.company_id.is_some() || self.title.is_some() || self.match_type.is_some() || self.reward.is_some() || self.reward_item_id.is_some() || self.currency.is_some() || self.stackable.is_some() || self.is_active.is_some()
    }
}

/// Repository trait for PromoBundle aggregate
///
/// This trait defines the persistence contract for PromoBundle entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait PromoBundleRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new promo_bundle entity
    async fn save(&self, entity: &PromoBundle) -> Result<PromoBundle>;

    /// Find promo_bundle by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<PromoBundle>>;

    /// Find all promo_bundle entities
    async fn find_all(&self) -> Result<Vec<PromoBundle>>;

    /// Update promo_bundle by ID
    async fn update(&self, id: &str, entity: &PromoBundle) -> Result<Option<PromoBundle>>;

    /// Delete promo_bundle by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List promo_bundle with pagination
    async fn list(&self, params: PromoBundlePaginationParams) -> Result<PromoBundlePaginatedResult>;

    /// List promo_bundle with pagination and filters
    async fn list_with_filters(&self, params: PromoBundlePaginationParams, filters: PromoBundleFilter) -> Result<PromoBundlePaginatedResult>;

    /// Count all promo_bundle entities
    async fn count(&self) -> Result<u64>;

    /// Count promo_bundle entities matching filters
    async fn count_with_filters(&self, filters: PromoBundleFilter) -> Result<u64>;

    /// Check if promo_bundle exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete promo_bundle by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted promo_bundle by ID
    async fn restore(&self, id: &str) -> Result<Option<PromoBundle>>;

    /// List soft-deleted promo_bundle entities
    async fn list_deleted(&self, params: PromoBundlePaginationParams) -> Result<PromoBundlePaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save promo_bundle entities
    async fn bulk_save(&self, entities: &[PromoBundle]) -> Result<Vec<PromoBundle>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
